@model PTAWMS.Models.Att_Setting

@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Create</h2>

@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>Att_Setting</h4>
        <hr />
        @Html.ValidationSummary(true)

        <div class="form-group">
            @Html.LabelFor(model => model.SupervisorCutOffDate, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.SupervisorCutOffDate)
                @Html.ValidationMessageFor(model => model.SupervisorCutOffDate)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.RecommendCutOffDate, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.RecommendCutOffDate)
                @Html.ValidationMessageFor(model => model.RecommendCutOffDate)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.ApprovedCutOffDate, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.ApprovedCutOffDate)
                @Html.ValidationMessageFor(model => model.ApprovedCutOffDate)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
